# Task Log

One row per IDD→SDD chain `<topic>`. Upserted by the `check-*` commands.

| Topic | Status | Intent | Spec | Plan | Result | Opened | Closed | Notes |
|---|---|---|---|---|---|---|---|---|
| iwiki-content-hash-freshness-port | done | n/a | ✓ | ✓ | OK | 2026-06-30 | 2026-06-30 | Port content-hash freshness from ai-wiki-plugin into plugin/iwiki; spec opens the chain (no intent) |
| iwiki-engine-entrypoint | done | n/a | ✓ | ✓ | OK | 2026-06-30 | 2026-06-30 | Canonical IWIKI_ENGINE_DIR entrypoint; spec opens the chain (no intent); plan validated OK (5/5 phases passed, 0 findings) |
| iwiki-plugin-to-mcp | done | n/a | ✓ | ✓ | OK | 2026-06-30 | 2026-06-30 | Migrate iwiki usage to MCP server + decommission in-repo plugin; result_check OK (11/11 tasks, 47-file diff, 0 MISSING/0 EXCESS); final whole-branch review (opus) Ready-to-merge YES, 0 critical; plugin disabled (only plugin/iwiki/ kept); .iwikiignore + marketplace + uv retained; PR deferred per user |
| iwiki-mcp-user-scope | in-progress | n/a | ✓ | ✓ | – | 2026-07-01 | | Register iwiki MCP via tracked secret-free .mcp.json + --mcp-config; proxy IWIKI_* from .claude_config (ICLAUDE_ prefix); IWIKI_COMMAND via `command -v`; 11 env vars verified vs README/config.py (${VAR:-default} for optional); spec opens the chain (no intent); check-spec OK (4/4, 0 findings); check-plan OK (5/5, 0 findings); 6 tasks TDD |
| caveman-artifact-consolidation | done | n/a | – | ✓ | OK | 2026-07-01 | 2026-07-01 | Consolidate all caveman artifacts into $CLAUDE_CONFIG_DIR/.caveman/ + SessionEnd cleanup hook + age-prune 7→5d + legacy migration; spec via brainstorming self-review (not /check-chain spec); check-plan OK (5/5, 1 INFO accepted); 6 tasks TDD (subagent-driven); result_check OK (6/6 DONE, 0 EXCESS/MISSING, 4/4 suites green); final whole-branch review (opus) READY TO MERGE, 1 Minor fixed (off-mode empty .caveman/); 7 commits a7bbe9a6..baedd370; PR into dev pending |
| loen-loop-engineering-plugin | done | n/a | ✓ | ✓ | OK | 2026-07-01 | 2026-07-02 | loen = self-contained publishable Loop Engineering plugin (plugin/loen/, iclaude marketplace, user scope); artifacts in docs/loen/<run-id>/, templates as plugin assets (.agent-loop removed); subagents planner/explorer/verifier (distinct models, isolated context, single-writer worker); skills loop-delivery + loen:audit; hook loop-guard hard-enforces scope+layout/naming per topic; spec via brainstorming, opens chain (no intent); check-spec OK (4/4 phases); check-plan OK (5/5 phases) + max-effort/format re-checks all closed; 10 tasks TDD; implementation via PR #72 (eb855bf6..15b9c6e5, 26 files); result_check OK 2026-07-02 (10/10 DONE, 0 MISSING; 1 WARNING excess settings model opus→fable accepted; re-verified: 5/5 suites green, version-sync OK, loen@iclaude enabled + runtime-loaded) |
| loen-repair-autoresearch | in-progress | n/a | ✓ | – | – | 2026-07-02 | | loen spec 2 (backlog step 1): skills loop-repair + loop-autoresearch as specializations over the MVP loop; research metrics as JSONL event streams (iter-NN/metrics.jsonl via $LOEN_METRICS_PATH, experiments.jsonl run stream, baseline=iter-00); log_experiment.py deterministic writer; +1 canon path; budget.max_experiments=5; plugin 0.2.0; spec via brainstorming, opens chain (no intent); check-spec OK (4/4 phases, 0 CRITICAL, 5 WARNING + 6 INFO all fixed inline); max-effort completeness re-check vs methodology + real MVP code: 27 findings (5 CRITICAL: revert/checkpoint mechanics, multi-primary rule, planner/verifier instruction home, asset reuse) — ALL closed in-spec (§4.2a checkpoints, §4.3 metric grammar, §5.5 dispatch contracts); roster CONFIRMED by user: planner=fable, verifier=opus, explorer=haiku; sources: parent spec §3/§15, methodology §2/§7.3 |
| statusline-fable-context-window | in-progress | n/a | ✓ | ✓ | – | 2026-07-02 | | Fix statusline: detect_real_context_window() doesn't know fable/mythos/sonnet-5 → 200K window instead of 1M (observed 📊 228K (114%) ⚠️ on Fable); + 📦 hit-rate %.0f rounds 99.5%+ to 100% → floor via int(); spec via brainstorming, opens chain (no intent); check-spec OK (4/4 phases, 0 CRITICAL, 2 INFO fixed inline); check-plan OK (5/5 phases, 0 CRITICAL, 3 INFO fixed inline); 6 tasks TDD; branch dev-fix-statusline-context-window from dev, PR into dev |
| loen-goal-loop-wrapper | backlog | – | – | – | – | | | loen backlog step 2: optional /goal + /loop wrapping accelerator over self-contained loop-delivery; source: design spec §15 |
| loen-verifier-microvm | backlog | – | – | – | – | | | loen backlog step 3: optional hardening — run verifier under iclaude microVM FS isolation; iclaude already ships microVM integration, reuse it; source: design spec §15 + plan executor notes |
| loen-governance-observability | backlog | – | – | – | – | | | loen backlog step 4 (optional, spec 3): governance/observability — traces, dashboards, failure taxonomy; tension with iclaude offline/PII posture, may be dropped; source: design spec §3 |
