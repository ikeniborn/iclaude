# Exclude test and example files from GitHub secret scanning
# These files contain intentionally fake/example API key patterns
# used for testing secret detection functionality
paths-ignore:
  - 'tests/**'
  - 'docs/patterns.json.example'
