{
  "$comment": "Input template for commit-and-push skill",
  "commit_and_push_input": {
    "$comment": "Навык для коммита и пуша в репозиторий с автоматической фиксацией изменений",
    "repository_path": "{{repository_path: string, absolute path to git repository}}",
    "commit_message": "{{commit_message: string, min 10 chars, max 200 chars}}",
    "files_to_stage": "{{files_to_stage: array of strings, optional, paths relative to repository root}}",
    "branch": "{{branch: string, optional, target branch name}}",
    "push_options": {
      "$comment": "Опции для git push",
      "force": "{{force: boolean, optional, default false}}",
      "set_upstream": "{{set_upstream: boolean, optional, default false}}",
      "remote": "{{remote: string, optional, default origin}}"
    },
    "validation": {
      "$comment": "Опции валидации перед коммитом",
      "check_uncommitted_changes": "{{check_uncommitted_changes: boolean, optional, default true}}",
      "verify_remote": "{{verify_remote: boolean, optional, default true}}"
    }
  }
}
