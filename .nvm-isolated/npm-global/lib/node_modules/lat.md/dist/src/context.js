const identity = (s) => s;
export const plainStyler = {
    bold: identity,
    dim: identity,
    red: identity,
    cyan: identity,
    white: identity,
    green: identity,
    yellow: identity,
    boldWhite: identity,
};
